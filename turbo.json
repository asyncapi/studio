{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["storybook-static/**", "build/**"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "cache": false,
      "persistent": true
    },
    "release": {
      "dependsOn": ["build"]
    },
    "test": {},
    "bump:version": {
      "cache": false
    }
  }
}
