# Migration `20200510163316-default-free-plan`

This migration has been generated by fmvilas at 5/10/2020, 4:33:16 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200510162820-add-plans..20200510163316-default-free-plan
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model apis {
   asyncapi          String
@@ -38,9 +38,9 @@
   featureFlags                Json?                 @map(name: "feature_flags")
   id                          Int                   @default(autoincrement()) @id
   name                        String
   creatorId                   Int                   @map(name: "creator_id")
-  planName                    String?               @map(name: "plan_name")
+  planName                    String?               @default("free") @map(name: "plan_name")
   invitations                 invitations[]
   organizationUsers           organizationsUsers[]
   projects                    projects[]
   creator                     users                 @relation(fields: [creatorId], references: [id])
@@ -78,9 +78,9 @@
   email                String                @unique
   featureFlags         Json?                 @map(name: "feature_flags")
   id                   Int                   @default(autoincrement()) @id
   username             String                @unique
-  planName             String?               @map(name: "plan_name")
+  planName             String?               @default("free") @map(name: "plan_name")
   apis                 apis[]
   invitations          invitations[]
   organizationsForUser organizationsUsers[]
   projects             projects[]
```


