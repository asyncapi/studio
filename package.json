{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "ds": "turbo run dev --filter=...design-system"
  },
  "devDependencies": {
    "turbo": "^1.10.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
