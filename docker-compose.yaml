services:
  async-studio:
    build: .
    container_name: events-api
    restart: on-failure
    ports:
      - "8000:80"
    volumes:
      - ./apps/studio/public/docs/asyncapi:/usr/share/nginx/html/docs/asyncapi
